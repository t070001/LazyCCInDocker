# 🚀 Claude Code + Docker 沙盒隔離 + Agnes AI 免費端點 — 開發進度

## ✅ 目前完成了什麼
1. Dockerfile 路徑錯誤已修復：`FROM ubuntu:24.04`
2. devcontainer.json 路徑錯誤已修復：`"image": "ubuntu:24.04"`
3. devcontainer.json postCreateCommand 路徑修正：`sh ./setup-claude.sh`（原寫錯為 `/` 根目錄）
4. `setup-claude.sh` 修復：shebang 移到第一行、`ANTHROPIC_BASE_URL` URL 補全為 `http://127.0.0.1:15721`、enabledPlugins 更新為實際已安裝的 plugins
5. Skills 就緒：`i-have-adhd`、`ponytail`、`grill-me` 全部安裝

## ⏳ 哪裡改到一半
1. `install-mcp-skills.ps1` 中的 `@modelcontextprotocol/*` npm 套件不在公共 registry 上，需改從 GitHub 手動 clone 安裝或改用 npx
2. MCP (sentry, dockerhub, kubernetes, brave-search) 應在 Docker 容器內用 Docker MCP Toolkit 安裝，本機不需安裝
3. Docker 容器內需要的 MCP：`server-sqlite`、`server-fetch` 已寫入 postCreateCommand

## 🔙 回來第一步要做什麼
1. 確認 Docker Desktop 在運行，執行 `docker run ... ubuntu:24.04` 啟動沙盒容器
2. 透過 VS Code Dev Containers attach 到容器，按 README-SETUP.md 一鍵安裝
3. 在容器內執行 Claude Code 並啟用 Skills 開發
